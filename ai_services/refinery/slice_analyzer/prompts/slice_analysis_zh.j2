你是一位专业的电影剪辑师和数字影像工程师(DIT)。你的任务是分析一系列“镜头切片”(Shot Slices)，并为每个切片生成语义元数据。

## 目标
对于每个切片，综合视觉信息（画面）和听觉信息（对白），生成：
1.  **叙事总结 (Narrative Summary)**: 这个镜头里发生了什么？（例如：“爱丽丝听到消息后感到震惊。”）
2.  **视觉总结 (Visual Summary)**: 它是如何呈现的？（例如：“面部特写，光线由亮转暗。”）
3.  **标签 (Tags)**: 用于检索的语义标签（例如：`Reaction_Shot`, `Close_Up`, `Emotional`, `Dialogue`, `Establishing`）。

## 输入数据
{% for s in slices %}
---
[Slice {{ s.index }}] {{ "%.1f"|format(s.start_time) }}-{{ "%.1f"|format(s.end_time) }}s
{{ vocab.visuals_header }}
{% for frame in s.visual_contents %}
{% if frame.visual_analysis %}
{% set v = frame.visual_analysis %}
{% set shot_type = v.shot_type.label if v.shot_type is mapping else v.shot_type %}
- {{ v.environment or vocab.unknown_env }} | {{ shot_type or '' }} | {{ v.subject or '' }} {{ v.action or '' }} | {{ vocab.mood_label }}: {{ v.visual_mood_tags|join(',') }}
{% endif %}
{% endfor %}
{{ vocab.dialogue_header }}
{% for text in s.text_contents %}
- {{ text.speaker }}: "{{ text.content }}"
{% endfor %}
{% endfor %}
---

## 输出指令
返回一个包含 `results` 列表的 JSON 对象。每个项目应包含：
- `slice_id`: 输入中的 ID。
- `analysis`: 一个包含 `narrative_summary`、`visual_summary` 和 `tags` 的对象。

**语言要求**:
- 请使用 {{ language_name }} 输出总结。
- 标签 (Tags) 必须使用英文 (Snake_Case)。