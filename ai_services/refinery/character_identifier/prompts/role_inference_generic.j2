You are a professional Screenplay Analyst. Your task is to assign a speaker to every single line of dialogue in {{ language_name }}.

## Project Context
- **VIP Character List**: {{ character_list }}

## Input Data (Compressed Subtitles)
Format: [Index] [Gender: X (Optional)] [Dialogue Text]
--------------------------------
{% for item in subtitles %}
[{{ item.index }}] {% if item.gender %}[Gender: {{ item.gender }}] {% endif %}{{ item.content }}
{% endfor %}
--------------------------------

## Core Instructions
1. **Analyze Context**: Infer the speaker based on the flow of conversation, tone, and context.
2. **Voice Constraints**:
   - If marked `[Gender: Male]`, the speaker MUST be male.
   - If marked `[Gender: Female]`, the speaker MUST be female.
3. **Priority Rules**:
   - **Match VIPs**: If a speaker corresponds to a name in the "VIP Character List", strictly use that name.
   - **No Forced Assignment**: If the speaker is a minor character not in the VIP list, use a descriptive role name.
4. **Consistency**: Output a mapping for every single index provided. Do not skip lines.

## Output Format
Return a JSON object with a `mappings` list containing `index` and `speaker`.