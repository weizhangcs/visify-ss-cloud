# docker-compose.test.yml
# -----------------------------------------------------------------------------
# 描述: 真机测试环境 (Staging/Test)
# 特性:
#   1. 使用 Dev 标签镜像 (crpi-...)
#   2. 生产级并发配置 (仿真压测)
#   3. 数据持久化到 ./test_data (与生产隔离)
# -----------------------------------------------------------------------------

services:
  # --- 基础设施 (暴露端口以便调试) ---
  db:
    image: crpi-34v4qt829vtet2cy.cn-hangzhou.personal.cr.aliyuncs.com/vss_base/postgres:16-alpine
    ports:
      - "5432:5432"
    # [Fix] 物理挂载时，为了避免根目录非空导致 initdb 失败，指定子目录存储数据
    environment:
      - PGDATA=/var/lib/postgresql/data/pgdata

  redis:
    image: crpi-34v4qt829vtet2cy.cn-hangzhou.personal.cr.aliyuncs.com/vss_base/redis:7-alpine
    ports:
      - "6379:6379"

  # --- 应用服务 (仿真运行) ---
  web:
    image: 10.168.1.23:4999/vss_cloud/cloud:v1.4.0-alpha.2.hotfix
    #image: crpi-34v4qt829vtet2cy.cn-hangzhou.personal.cr.aliyuncs.com/vss_cloud/cloud:v1.4.0-alpha.2.hotfix
    command: gunicorn core.wsgi:application --bind 0.0.0.0:8000
    expose:
      - 8000
    volumes:
      - ./test_data/staticfiles:/app/staticfiles
      - ./test_data/shared_media:/app/shared_media
    restart: unless-stopped

  # --- Workers (生产级并发) ---
  worker-gemini:
    image: 10.168.1.23:4999/vss_cloud/cloud:v1.4.0-alpha.2.hotfix
    #image: crpi-34v4qt829vtet2cy.cn-hangzhou.personal.cr.aliyuncs.com/vss_cloud/cloud:v1.4.0-alpha.2.hotfix
    restart: unless-stopped
    command: celery -A core worker -Q queue-gemini -c 2 -l info
    volumes:
      - ./test_data/shared_media:/app/shared_media

  worker-audio:
    image: 10.168.1.23:4999/vss_cloud/cloud:v1.4.0-alpha.2.hotfix
    #image: crpi-34v4qt829vtet2cy.cn-hangzhou.personal.cr.aliyuncs.com/vss_cloud/cloud:v1.4.0-alpha.2.hotfix
    restart: unless-stopped
    command: celery -A core worker -Q queue-audio -c 4 -l info
    volumes:
      - ./test_data/shared_media:/app/shared_media

  worker-io:
    image: 10.168.1.23:4999/vss_cloud/cloud:v1.4.0-alpha.2.hotfix
    #image: crpi-34v4qt829vtet2cy.cn-hangzhou.personal.cr.aliyuncs.com/vss_cloud/cloud:v1.4.0-alpha.2.hotfix
    restart: unless-stopped
    command: celery -A core worker -Q queue-io,celery -c 8 -l info
    volumes:
      - ./test_data/shared_media:/app/shared_media

  # --- 网关 ---
  nginx:
    image: crpi-34v4qt829vtet2cy.cn-hangzhou.personal.cr.aliyuncs.com/vss_base/nginx:1.25-alpine
    ports:
      - "8080:80" # 测试环境映射到 8080
    volumes:
      - ./conf/nginx.template.conf:/etc/nginx/conf.d/default.conf.template
      - ./test_data/staticfiles:/app/staticfiles:ro
    depends_on:
      - web
    environment:
      - SERVER_DOMAIN
      - TZ=Asia/Shanghai
    entrypoint: /bin/sh -c "envsubst '$$SERVER_DOMAIN' < /etc/nginx/conf.d/default.conf.template > /etc/nginx/conf.d/default.conf && nginx -g 'daemon off;'"
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "nginx", "-t"]
      interval: 10s
      timeout: 5s
      retries: 3

# --- 物理挂载 (Test Data) ---
volumes:
  postgres_data:
    driver: local
    driver_opts:
      type: none
      o: bind
      device: ./test_data/postgres
  redis_data:
    driver: local
    driver_opts:
      type: none
      o: bind
      device: ./test_data/redis